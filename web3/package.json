{
  "name": "health-insurance-dapp",
  "version": "1.0.0",
  "description": "Decentralized Health Insurance Platform",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "deploy:holesky": "hardhat run scripts/deploy.js --network holesky",
    "node": "hardhat node",
    "verify:holesky": "hardhat verify --network holesky",
    "console:localhost": "hardhat console --network localhost",
    "console:holesky": "hardhat console --network holesky",
    "clear": "rm -rf node_modules package-lock.json cache deployments"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "chai": "^4.5.0",
    "dotenv": "^16.6.1",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.0",
    "hardhat-gas-reporter": "^1.0.10",
    "solidity-coverage": "^0.8.12"
  },
  "keywords": [
    "blockchain",
    "insurance",
    "defi",
    "hardhat",
    "solidity"
  ],
  "author": "Your Name",
  "license": "MIT"
}
